import {SERVER_HOST} from '~/constants/variable';

// EX : [BASEURL (String), METHOD (String), ENDPOINT (String), ADD ON URL (Boolean)]

// User
export const UsersAPI = [SERVER_HOST, 'get', '/api', true];

